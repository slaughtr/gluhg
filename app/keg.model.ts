export class Keg {
  constructor(public name: string, public brewery: string, public price: number, public abv: number, public ibu: number, public style: string, public pintsConsumed: number, public pintsCapacity: number, public imgURL: string) {}
}
